# Claude Memory

- Project: design-awards-inspiration now lives at `/Users/cdmxx/Documents/design-awards-inspiration` and includes the award-winning seed set, UI scripts, and helper tools.
- Active branch: match the repo's current branch (`master` by default) and keep Claude/Codex synced before editing.
- Focus areas: keep the dataset clean, keep `.gitignore` and workflow docs current, and avoid breaking the preview UI.
- Recent work: moved the repo out of the hidden skill folder and created the coordination docs plus the shared workflow ritual.
- Reminder: update `NEXT_STEPS.md`, `HANDOFF_TEMPLATE.md`, and `WORKFLOW.md` before handing back to Codex.
